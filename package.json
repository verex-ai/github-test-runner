{
  "name": "@verex/github-test-runner",
  "version": "1.0.0",
  "description": "GitHub Action to run tests via an API with polling and status checking",
  "main": "run-tests.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "github",
    "actions",
    "testing",
    "api"
  ],
  "author": "Verex",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.2"
  }
}
